- [x] 账单录入回车需要提交表单 — 已通过 `<form onSubmit>` 原生回车提交修复（TransactionForm / QuickAddDialog）
- [x] 账单列表页加载很慢，尤其是快速滑动时，后面的数据很久才能出来 — 已优化：
  - 后端分页取消每页 COUNT 全表扫描，改为多取一行判定 hasNext（totalCount/totalPages 不再返回）
  - 单页条数 50 → 100，减少 IPC 往返
  - 移除 AppShell 每次路由变化的全量 invalidateQueries（与 refetchOnMount: "always" 冗余）
  - 此前已完成的优化：React.memo、useCallback、虚拟列表 rootMargin 200px
